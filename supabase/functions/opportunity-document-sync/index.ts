import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";

const U = Deno.env.get("SUPABASE_URL")!;
const S = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const CORS = { "Access-Control-Allow-Origin": "*", "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type" };
const json = (body: unknown, status = 200) => new Response(JSON.stringify(body), { status, headers: { ...CORS, "content-type": "application/json" } });
const safe = (v: string) => v.normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/[^a-zA-Z0-9._-]/g, "_").slice(0, 180);
function parsePncp(url: string) { try { const u=new URL(url); if(u.hostname!=="pncp.gov.br") return null; const m=u.pathname.match(/^\/app\/editais\/(\d{14})\/(\d{4})\/(\d+)\/?$/); return m?{cnpj:m[1],ano:m[2],sequencial:String(Number(m[3]))}:null; } catch { return null; } }
function officialHttps(url:string){ try{const u=new URL(url);return u.protocol==="https:"&&(u.hostname==="pncp.gov.br"||u.hostname.endsWith(".gov.br"));}catch{return false;} }
function mimeFor(ct0:string|null,name:string){const ct=(ct0||"").split(";")[0].trim().toLowerCase();const allowed=new Set(["application/pdf","application/zip","application/x-zip-compressed","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document","application/vnd.ms-excel","application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"]);if(allowed.has(ct))return ct;const n=name.toLowerCase();if(n.endsWith(".pdf"))return"application/pdf";if(n.endsWith(".zip"))return"application/zip";if(n.endsWith(".doc"))return"application/msword";if(n.endsWith(".docx"))return"application/vnd.openxmlformats-officedocument.wordprocessingml.document";if(n.endsWith(".xls"))return"application/vnd.ms-excel";if(n.endsWith(".xlsx"))return"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet";return null;}
function filenameFrom(res:Response,fallback:string){const cd=res.headers.get("content-disposition")||"";const utf=cd.match(/filename\*=UTF-8''([^;]+)/i),plain=cd.match(/filename="?([^";]+)"?/i);try{if(utf?.[1])return decodeURIComponent(utf[1]);}catch{}return plain?.[1]||fallback;}
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:CORS});if(req.method!=="POST")return json({error:"method_not_allowed"},405);const auth=req.headers.get("authorization")||"";if(!auth.startsWith("Bearer "))return json({error:"unauthorized"},401);const token=auth.slice(7);const db=createClient(U,S,{auth:{persistSession:false,autoRefreshToken:false}});const {data:userData,error:userError}=await db.auth.getUser(token);const user=userData.user;if(userError||!user)return json({error:"unauthorized"},401);const body=await req.json().catch(()=>({}));const clientId=String(body.client_id||""),opportunityId=String(body.opportunity_id||"");if(!clientId||!opportunityId)return json({error:"client_and_opportunity_required"},400);const {data:member}=await db.from("client_members").select("role").eq("client_id",clientId).eq("user_id",user.id).maybeSingle();if(!member)return json({error:"forbidden"},403);const {data:match}=await db.from("client_opportunity_matches").select("opportunity_id").eq("client_id",clientId).eq("opportunity_id",opportunityId).limit(1).maybeSingle();if(!match)return json({error:"opportunity_not_available_for_client"},403);const {data:op}=await db.from("public_opportunities").select("source_url").eq("id",opportunityId).maybeSingle();const sourceUrl=String(op?.source_url||"");const pncp=parsePncp(sourceUrl);if(!pncp)return json({ok:true,status:"manual_required",reason:"pncp_source_not_parseable",source_url:sourceUrl,downloaded:0,failed:0});const listUrl=`https://pncp.gov.br/api/pncp/v1/orgaos/${pncp.cnpj}/compras/${pncp.ano}/${pncp.sequencial}/arquivos`;let listRes:Response;try{listRes=await fetch(listUrl,{headers:{accept:"application/json"},signal:AbortSignal.timeout(15000)});}catch(e){return json({ok:true,status:"manual_required",reason:`document_list_fetch_failed:${String(e)}`,source_url:sourceUrl,downloaded:0,failed:0});}if(!listRes.ok)return json({ok:true,status:"manual_required",reason:`document_list_http_${listRes.status}`,source_url:sourceUrl,downloaded:0,failed:0});const payload=await listRes.json().catch(()=>null);const docs=Array.isArray(payload)?payload:Array.isArray(payload?.data)?payload.data:Array.isArray(payload?.documentos)?payload.documentos:[];if(!docs.length)return json({ok:true,status:"manual_required",reason:"no_documents_returned_by_pncp",source_url:sourceUrl,downloaded:0,failed:0});let downloaded=0,skipped=0,failed=0;const results:unknown[]=[];for(const d of docs.slice(0,25)){const seq=String(d.sequencialDocumento??d.sequencial??d.id??"");const title=String(d.titulo??d.nomeArquivo??`documento-${seq||downloaded+failed+1}`);const {data:existing}=await db.from("opportunity_documents").select("id,original_filename").eq("client_id",clientId).eq("opportunity_id",opportunityId).contains("metadata",{pncp_sequencial_documento:seq}).limit(1).maybeSingle();if(existing){skipped++;results.push({sequencial:seq,status:"already_available",filename:existing.original_filename});continue;}let docUrl=String(d.url??d.urlDownload??"");if(!docUrl&&seq)docUrl=`${listUrl}/${encodeURIComponent(seq)}`;if(!officialHttps(docUrl)){failed++;results.push({sequencial:seq,status:"manual_required",reason:"non_official_download_host"});continue;}try{let res=await fetch(docUrl,{headers:{accept:"*/*"},redirect:"follow",signal:AbortSignal.timeout(25000)});if(!res.ok)throw new Error(`http_${res.status}`);const ctype=res.headers.get("content-type")||"";if(ctype.includes("application/json")){const p=await res.json();const next=String(p?.urlDownload??p?.url??"");if(!officialHttps(next))throw new Error("download_url_not_official");res=await fetch(next,{headers:{accept:"*/*"},redirect:"follow",signal:AbortSignal.timeout(25000)});if(!res.ok)throw new Error(`download_http_${res.status}`);}const provisional=safe(title)||`documento-${seq}`;const name=safe(filenameFrom(res,provisional));const mime=mimeFor(res.headers.get("content-type"),name);if(!mime)throw new Error("unsupported_file_type");const bytes=new Uint8Array(await res.arrayBuffer());if(!bytes.byteLength||bytes.byteLength>50*1024*1024)throw new Error("invalid_file_size");const finalName=/\.(pdf|zip|doc|docx|xls|xlsx)$/i.test(name)?name:`${name}${mime==="application/pdf"?".pdf":""}`;const path=`${clientId}/${opportunityId}/pncp-${safe(seq||crypto.randomUUID())}-${finalName}`;const {error:uploadError}=await db.storage.from("opportunity-documents").upload(path,bytes,{contentType:mime,upsert:false});if(uploadError)throw uploadError;const {error:insertError}=await db.from("opportunity_documents").insert({client_id:clientId,opportunity_id:opportunityId,storage_bucket:"opportunity-documents",storage_path:path,original_filename:finalName,mime_type:mime,file_size_bytes:bytes.byteLength,source_kind:"agent_download",validation_status:"available",uploaded_by:user.id,metadata:{origin:"pncp_auto_download",pncp_sequencial_documento:seq,pncp_title:title,pncp_download_url:docUrl,absorption_state:"available_for_analysis"}});if(insertError){await db.storage.from("opportunity-documents").remove([path]);throw insertError;}downloaded++;results.push({sequencial:seq,status:"downloaded",filename:finalName,size:bytes.byteLength});}catch(e){failed++;results.push({sequencial:seq,status:"manual_required",reason:e instanceof Error?e.message:String(e)});}}const status=downloaded>0||skipped>0?(failed>0?"partial":"complete"):"manual_required";return json({ok:true,status,source_url:sourceUrl,found:docs.length,downloaded,skipped,failed,results});});