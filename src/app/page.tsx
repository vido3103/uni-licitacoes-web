"use client";
import {useEffect,useState} from "react";
import AuthGate from "@/components/AuthGate";import BackendStatus from "@/components/BackendStatus";import Sidebar from "@/components/Sidebar";import UniAiPanel from "@/components/UniAiPanel";import {supabase} from "@/lib/supabase";
import Dashboard from "@/modules/dashboard/Dashboard";import Radar from "@/modules/radar/Radar";import Oportunidades from "@/modules/oportunidades/Oportunidades";import Negocios from "@/modules/negocios/Negocios";import CFP from "@/modules/cfp/CFP";import GateEconomico from "@/modules/gates/GateEconomico";import GateParticipacao from "@/modules/gates/GateParticipacao";import Disputa from "@/modules/disputa/Disputa";import Relatorios from "@/modules/relatorios/Relatorios";import Documentos from "@/modules/documentos/Documentos";import Fornecedores from "@/modules/fornecedores/Fornecedores";import Configuracoes from "@/modules/configuracoes/Configuracoes";import CompanyOnboarding from "@/modules/onboarding/CompanyOnboarding";import CompanyRegistryReview from "@/modules/onboarding/CompanyRegistryReview";import SicafHabilitacao from "@/modules/sicaf/SicafHabilitacao";import OwnerAdmin from "@/modules/admin/OwnerAdmin";
type Note={id:string;title:string;detail:string;kind:"pending"|"analysis";createdAt?:string|null};
export default function Home(){const[active,setActive]=useState("Painel"),[owner,setOwner]=useState(false),[selected,setSelected]=useState(false),[ready,setReady]=useState(false),[mobile,setMobile]=useState(false),[ai,setAi]=useState(false),[notes,setNotes]=useState<Note[]>([]),[notesOpen,setNotesOpen]=useState(false),[query,setQuery]=useState(""),[signing,setSigning]=useState(false);
 useEffect(()=>{if(!supabase)return;void(async()=>{try{const{data}=await supabase.rpc("get_my_uni_identity");const x=data as{platform_role?:string;memberships?:unknown[]}|null;const isOwner=x?.platform_role==="platform_owner";setOwner(isOwner);const has=isOwner&&Boolean(sessionStorage.getItem("uni-owner-client-id"));setSelected(has);setActive(isOwner&&!has?"Administração":!isOwner&&Array.isArray(x?.memberships)&&x!.memberships!.length===0?"Empresa":"Painel")}finally{setReady(true)}})()},[]);
 useEffect(()=>{const h=(e:Event)=>{const m=(e as CustomEvent<string>).detail;if(m)navigate(m)};window.addEventListener("uni-navigate",h);return()=>window.removeEventListener("uni-navigate",h)},[owner,selected]);
 async function loadNotes(){if(!supabase||owner&&!selected)return;let cid=sessionStorage.getItem("uni-owner-client-id");if(!cid){const{data:u}=await supabase.auth.getUser();if(u.user){const{data:m}=await supabase.from("client_members").select("client_id").eq("user_id",u.user.id).limit(1).maybeSingle();cid=m?.client_id?String(m.client_id):null}}if(!cid)return;const[p,a]=await Promise.all([supabase.from("client_pending_items").select("id,title,description,impact,created_at").eq("client_id",cid).not("state","in","(resolvida,cancelada)").order("created_at",{ascending:false}).limit(5),supabase.from("opportunity_ai_analysis_queue").select("id,status,queued_at,error_detail").eq("client_id",cid).in("status",["pending","processing","failed","retry_wait"]).order("queued_at",{ascending:false}).limit(5)]);const n:Note[]=[...(p.data??[]).map(x=>({id:`p-${x.id}`,title:String(x.title||"Ação necessária"),detail:String(x.description||x.impact||"Revise esta pendência."),kind:"pending" as const,createdAt:x.created_at})),...(a.data??[]).map(x=>({id:`a-${x.id}`,title:x.status==="failed"?"Análise precisa de revisão":"Análise em andamento",detail:x.status==="failed"?"O UNI não concluiu esta análise. Abra a oportunidade para revisar.":"O UNI está processando a oportunidade.",kind:"analysis" as const,createdAt:x.queued_at}))];setNotes(n.slice(0,8))}
 function navigate(m:string){if(owner&&!selected){setActive(m);setMobile(false);return}if(m==="Administração"&&owner){leave();return}setActive(m);setMobile(false)}
 function openClient(id:string){sessionStorage.setItem("uni-owner-client-id",id);setSelected(true);setActive("Painel");setNotes([])}function leave(){sessionStorage.removeItem("uni-owner-client-id");setSelected(false);setActive("Administração");setNotes([])}
 async function signout(){if(!supabase||signing)return;setSigning(true);sessionStorage.removeItem("uni-owner-client-id");await supabase.auth.signOut();setSigning(false)}
 function search(e:React.FormEvent){e.preventDefault();const q=query.trim();if(!q)return;if(owner&&!selected){setActive("Oportunidades");return}sessionStorage.setItem("uni-global-search",q);setActive("Oportunidades")}
 function content(){if(!ready)return <div className="p-8 text-sm text-slate-500">Carregando ambiente UNI...</div>;if(owner&&!selected)return <OwnerAdmin section={active} onOpenClient={openClient}/>;switch(active){case"Painel":case"Dashboard":return <Dashboard onNavigate={navigate}/>;case"Oportunidades":case"Editais":return <Oportunidades/>;case"Radar":return <Radar/>;case"Negócios":return <Negocios onNavigate={navigate}/>;case"CFP":return <CFP/>;case"Gate Econômico":return <GateEconomico/>;case"Disputa":return <Disputa/>;case"Inteligência":case"Relatórios":return <Relatorios/>;case"Empresa":case"Empresas":return <><CompanyOnboarding/><CompanyRegistryReview/></>;case"SICAF":return <SicafHabilitacao/>;case"Gate de Participação":return <GateParticipacao/>;case"Documentos":return <Documentos/>;case"Fornecedores":return <Fornecedores/>;case"Configurações":return <Configuracoes/>;default:return <Dashboard onNavigate={navigate}/>}}
 const admin=owner&&!selected;const mobileItems=admin?["Administração","Clientes","Oportunidades","Negócios","Inteligência","Habilitação","Documentos","Usuários","Relatórios","Configurações"]:["Painel","Oportunidades","Negócios","Inteligência","Empresa",...(owner?["Administração"]:[])];
 return <AuthGate><main className="min-h-screen bg-[#f4f8fc] text-slate-900"><div className="flex min-h-screen"><Sidebar active={active} onNavigate={navigate} ownerWorkspaceOnly={admin} isPlatformOwner={owner}/><div className="min-w-0 flex-1"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="flex h-16 items-center gap-2.5 px-3 sm:px-4 xl:px-6"><button onClick={()=>setMobile(!mobile)} className="rounded-lg border px-2.5 py-1.5 text-sm lg:hidden">☰</button><div className="hidden min-w-[190px] lg:block"><p className="text-xs font-bold">{admin?"Owner UNI":"UNI Licitações"}</p><p className="text-[10px] text-slate-500">{admin?"Administração da plataforma":"o UNI opera o método; você decide"}</p></div><form onSubmit={search} className="relative mx-auto w-full max-w-[500px]"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={admin?"Buscar na administração":"Buscar oportunidades, órgãos, processos..."} className="w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs"/></form><button onClick={()=>setAi(!ai)} className="hidden rounded-lg border border-blue-200 bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700 sm:block">✦ Fale com a IA</button>{!admin&&<div className="relative hidden sm:block"><button onClick={()=>{setNotesOpen(!notesOpen);if(!notesOpen)void loadNotes()}} className="relative h-9 w-9 rounded-lg border">🔔{notes.length>0&&<span className="absolute -right-1 -top-1 rounded-full bg-rose-500 px-1 text-[9px] text-white">{notes.length}</span>}</button>{notesOpen&&<div className="absolute right-0 top-11 w-80 rounded-xl border bg-white shadow-xl"><div className="border-b p-3 text-xs font-bold">Notificações</div>{notes.length?notes.map(n=><button key={n.id} onClick={()=>{setNotesOpen(false);navigate(n.kind==="analysis"?"Oportunidades":"Empresa")}} className="block w-full border-b p-3 text-left"><b className="text-xs">{n.title}</b><p className="mt-1 text-[10px] text-slate-500">{n.detail}</p></button>):<p className="p-4 text-xs text-slate-500">Nenhuma ação pendente.</p>}</div>}</div>}<div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-full bg-blue-600 text-xs font-black text-white">{owner?"ON":"U"}</span>{owner&&selected&&<button onClick={leave} className="hidden rounded-lg border border-blue-200 bg-blue-50 px-2.5 py-2 text-[10px] font-bold text-blue-700 xl:block">Sair do cliente</button>}<button onClick={()=>void signout()} disabled={signing} className="rounded-lg border px-2.5 py-2 text-[10px] font-bold text-slate-600">{signing?"Saindo...":"Sair"}</button></div></div>{mobile&&<div className="grid grid-cols-2 gap-2 border-t p-3 lg:hidden">{mobileItems.map(x=><button key={x} onClick={()=>navigate(x)} className={`rounded-lg px-3 py-2 text-left text-xs font-bold ${active===x?"bg-blue-600 text-white":"bg-slate-50"}`}>{x}</button>)}</div>}</header><BackendStatus/><section>{content()}</section></div></div><UniAiPanel open={ai} onClose={()=>setAi(false)} activeModule={active} onNavigate={navigate}/></main></AuthGate>}
