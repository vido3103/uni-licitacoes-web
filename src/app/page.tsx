"use client";

import { useEffect, useState } from "react";
import AuthGate from "@/components/AuthGate";
import BackendStatus from "@/components/BackendStatus";
import Sidebar from "@/components/Sidebar";
import { supabase } from "@/lib/supabase";
import Dashboard from "@/modules/dashboard/Dashboard";
import Radar from "@/modules/radar/Radar";
import Editais from "@/modules/editais/Editais";
import CFP from "@/modules/cfp/CFP";
import GateEconomico from "@/modules/gates/GateEconomico";
import Disputa from "@/modules/disputa/Disputa";
import Relatorios from "@/modules/relatorios/Relatorios";
import Documentos from "@/modules/documentos/Documentos";
import Fornecedores from "@/modules/fornecedores/Fornecedores";
import Configuracoes from "@/modules/configuracoes/Configuracoes";
import CompanyOnboarding from "@/modules/onboarding/CompanyOnboarding";
import CompanyRegistryReview from "@/modules/onboarding/CompanyRegistryReview";
import SicafHabilitacao from "@/modules/sicaf/SicafHabilitacao";

type HeaderNotification = { id:string; title:string; detail:string; kind:"pending"|"analysis"; createdAt?:string|null };
function notificationTime(value?:string|null){if(!value)return"";const d=new Date(value);if(Number.isNaN(d.getTime()))return"";return new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(d)}

export default function Home(){
  const [active,setActive]=useState("Dashboard");
  const [signingOut,setSigningOut]=useState(false);
  const [notificationsOpen,setNotificationsOpen]=useState(false);
  const [notificationsLoading,setNotificationsLoading]=useState(false);
  const [notifications,setNotifications]=useState<HeaderNotification[]>([]);
  const [globalQuery,setGlobalQuery]=useState("");
  const [mobileOpen,setMobileOpen]=useState(false);
  async function loadNotifications(){if(!supabase)return;setNotificationsLoading(true);try{const {data:auth}=await supabase.auth.getUser();if(!auth.user){setNotifications([]);return}const {data:membership,error:membershipError}=await supabase.from("client_members").select("client_id").eq("user_id",auth.user.id).limit(1).maybeSingle();if(membershipError||!membership?.client_id){setNotifications([]);return}const [pendingResponse,queueResponse]=await Promise.all([supabase.from("client_pending_items").select("id,title,description,impact,state,created_at").eq("client_id",membership.client_id).not("state","in","(resolvida,cancelada)").order("created_at",{ascending:false}).limit(5),supabase.from("opportunity_ai_analysis_queue").select("id,status,prompt_master_version,queued_at,completed_at,error_detail").eq("client_id",membership.client_id).in("status",["pending","processing","failed","retry_wait"]).order("queued_at",{ascending:false}).limit(5)]);const pending:HeaderNotification[]=(pendingResponse.data??[]).map(row=>({id:`pending-${row.id}`,title:String(row.title||"Pendência do cadastro"),detail:row.description?String(row.description):`Impacto: ${String(row.impact||"a verificar")}`,kind:"pending",createdAt:row.created_at}));const queue:HeaderNotification[]=(queueResponse.data??[]).map(row=>({id:`analysis-${row.id}`,title:row.status==="failed"?"Falha na Análise Detalhada":row.status==="processing"?"Análise Detalhada em execução":"Análise Detalhada aguardando execução",detail:row.status==="failed"?String(row.error_detail||"A execução precisa ser revisada."):`Execução registrada com ${String(row.prompt_master_version||"Prompt Mestre")}.`,kind:"analysis",createdAt:row.queued_at}));setNotifications([...queue,...pending].sort((a,b)=>new Date(b.createdAt||0).getTime()-new Date(a.createdAt||0).getTime()).slice(0,8));}finally{setNotificationsLoading(false)}}
  useEffect(()=>{void loadNotifications()},[active]);useEffect(()=>{if(!supabase)return;void(async()=>{const{data,error}=await supabase.rpc("get_my_uni_identity");if(error||!data)return;const identity=data as{platform_role?:string|null;memberships?:unknown[]};if(identity.platform_role!=="platform_owner"&&Array.isArray(identity.memberships)&&identity.memberships.length===0)setActive("Empresas")})()},[]);
  async function handleSignOut(){if(!supabase||signingOut)return;setSigningOut(true);await supabase.auth.signOut();setSigningOut(false)}function openNotification(n:HeaderNotification){setNotificationsOpen(false);setActive(n.kind==="analysis"?"Radar":"Configurações")}function navigate(module:string){setActive(module);setMobileOpen(false)}function submitGlobalSearch(e:React.FormEvent){e.preventDefault();const q=globalQuery.trim();if(!q)return;sessionStorage.setItem("uni-global-search",q);setActive("Editais")}function renderContent(){switch(active){case"Dashboard":return <Dashboard onNavigate={navigate}/>;case"Empresas":return <><CompanyOnboarding/><CompanyRegistryReview/></>;case"SICAF":return <SicafHabilitacao/>;case"Radar":return <Radar/>;case"Editais":return <Editais/>;case"CFP":return <CFP/>;case"Gate Econômico":return <GateEconomico/>;case"Disputa":return <Disputa/>;case"Relatórios":return <Relatorios/>;case"Documentos":return <Documentos/>;case"Fornecedores":return <Fornecedores/>;case"Configurações":return <Configuracoes/>;default:return <Dashboard onNavigate={navigate}/>}}const mobileItems=["Dashboard","Empresas","SICAF","Radar","Editais","CFP","Gate Econômico","Disputa","Relatórios","Documentos","Fornecedores","Configurações"];
  return <AuthGate><main className="min-h-screen bg-[#f4f8fc] text-slate-900"><div className="flex min-h-screen"><Sidebar active={active} onNavigate={navigate}/><div className="min-w-0 flex-1"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="flex h-[76px] items-center gap-4 px-4 sm:px-6 xl:px-8"><button type="button" onClick={()=>setMobileOpen(v=>!v)} className="rounded-lg border border-slate-200 px-3 py-2 text-sm lg:hidden" aria-label="Abrir menu">☰</button><div className="hidden min-w-[260px] lg:block"><p className="text-sm font-semibold text-slate-900">Inteligência em Licitações</p><p className="text-xs text-slate-500">para o seu resultado</p></div><form onSubmit={submitGlobalSearch} className="relative mx-auto w-full max-w-[560px]"><span className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-slate-400">⌕</span><input value={globalQuery} onChange={e=>setGlobalQuery(e.target.value)} type="search" placeholder="Buscar editais, órgãos, processos, palavras-chave..." className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-10 pr-4 text-sm outline-none transition focus:border-blue-400 focus:bg-white focus:ring-4 focus:ring-blue-100"/></form><div className="relative hidden sm:block"><button type="button" onClick={()=>{const next=!notificationsOpen;setNotificationsOpen(next);if(next)void loadNotifications()}} aria-label="Notificações" className="relative flex h-10 w-10 items-center justify-center rounded-xl border border-slate-200 text-slate-600 hover:bg-slate-50">🔔{notifications.length>0&&<span className="absolute -right-1 -top-1 flex h-5 min-w-5 items-center justify-center rounded-full bg-rose-500 px-1 text-[10px] font-bold text-white">{notifications.length>9?"9+":notifications.length}</span>}</button>{notificationsOpen&&<div className="absolute right-0 top-12 w-[380px] overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-xl"><div className="flex items-center justify-between border-b border-slate-100 px-4 py-3"><div><p className="text-sm font-bold">Notificações</p><p className="text-xs text-slate-500">Pendências e execuções do seu ambiente</p></div><button onClick={()=>void loadNotifications()} className="text-xs font-semibold text-blue-600">Atualizar</button></div><div className="max-h-[420px] overflow-y-auto">{notificationsLoading?<div className="p-5 text-sm text-slate-500">Atualizando notificações...</div>:notifications.length===0?<div className="p-5 text-sm text-slate-500">Nenhuma notificação pendente no momento.</div>:notifications.map(n=><button key={n.id} onClick={()=>openNotification(n)} className="block w-full border-b border-slate-100 px-4 py-3 text-left hover:bg-slate-50"><p className="text-sm font-semibold">{n.title}</p><p className="mt-1 line-clamp-2 text-xs text-slate-500">{n.detail}</p>{n.createdAt&&<p className="mt-1 text-[10px] text-slate-400">{notificationTime(n.createdAt)}</p>}</button>)}</div></div>}</div><div className="hidden h-8 w-px bg-slate-200 sm:block"/><div className="flex shrink-0 items-center gap-2"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-blue-600 text-xs font-bold text-white">U</span><button onClick={handleSignOut} disabled={signingOut} className="ml-1 inline-flex h-9 items-center rounded-lg border border-slate-200 px-3 text-xs font-semibold text-slate-600 disabled:opacity-50">{signingOut?"Saindo...":"Sair"}</button></div></div>{mobileOpen&&<div className="border-t border-slate-100 bg-white p-3 lg:hidden"><div className="grid grid-cols-2 gap-2">{mobileItems.map(item=><button key={item} onClick={()=>navigate(item)} className={`rounded-lg px-3 py-2 text-left text-xs font-semibold ${active===item?"bg-blue-600 text-white":"bg-slate-50 text-slate-700"}`}>{item}</button>)}</div></div>}</header><BackendStatus/><section>{renderContent()}</section></div></div></main></AuthGate>;
}
