"use client";
import{useEffect,useMemo,useState}from"react";import{supabase}from"@/lib/supabase";type Company={client_id:string;display_name:string;legal_name:string;status:string;company_validation_status:string|null};type Level={id:number;code:string;name:string;description:string|null;status:string;observations:string|null;last_checked_at:string|null;evidence_document_id:string|null};type Workspace={client_id:string;company_validated:boolean;company_validation_status:string;levels:Level[];checkpoint:{status:string;completion_percent:number;blocking_reason:string|null}|null};const labels:Record<string,string>={nao_iniciado:"Não iniciado",em_preparacao:"Em preparação",pendente:"Pendente",atende:"Atende",impeditivo:"Impeditivo"};function err(e:unknown){if(e instanceof Error)return e.message;if(e&&typeof e==="object"&&"message"in e)return String((e as{message?:unknown}).message||"Erro");return String(e||"Erro")}
export default function SicafHabilitacao(){const[companies,setCompanies]=useState<Company[]>([]);const[clientId,setClientId]=useState("");const[workspace,setWorkspace]=useState<Workspace|null>(null);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState<number|null>(null);const[error,setError]=useState("");const[msg,setMsg]=useState("");const[notes,setNotes]=useState<Record<number,string>>({});const selected=useMemo(()=>companies.find(c=>c.client_id===clientId),[companies,clientId]);async function loadCompanies(){if(!supabase)return;setLoading(true);setError("");try{const{data,error}=await supabase.functions.invoke("sicaf-workspace",{body:{action:"list"}});if(error)throw error;if(!data?.ok)throw new Error(data?.detail||"Falha ao carregar empresas.");const rows=(data.companies??[])as Company[];setCompanies(rows);if(rows.length&&!clientId)setClientId(rows[0].client_id)}catch(e){setError(err(e))}finally{setLoading(false)}}async function loadWorkspace(id=clientId){if(!supabase||!id)return;setLoading(true);setError("");try{const{data,error}=await supabase.functions.invoke("sicaf-workspace",{body:{action:"workspace",client_id:id}});if(error)throw error;if(!data?.ok)throw new Error(data?.detail||"Falha ao carregar SICAF.");const w=data.workspace as Workspace;setWorkspace(w);setNotes(Object.fromEntries(w.levels.map(l=>[l.id,l.observations||""])))}catch(e){setWorkspace(null);setError(err(e))}finally{setLoading(false)}}useEffect(()=>{void loadCompanies()},[]);useEffect(()=>{if(clientId)void loadWorkspace(clientId)},[clientId]);async function setStatus(level:Level,status:string){if(!supabase||!workspace)return;setSaving(level.id);setError("");setMsg("");try{const{data,error}=await supabase.functions.invoke("sicaf-workspace",{body:{action:"set",client_id:workspace.client_id,level_id:level.id,status,observations:notes[level.id]||null,evidence_document_id:level.evidence_document_id}});if(error)throw error;if(!data?.ok)throw new Error(data?.detail||"Falha ao atualizar nível SICAF.");setMsg(`Nível ${level.id} atualizado para ${labels[status]||status}.`);await loadWorkspace(workspace.client_id)}catch(e){setError(err(e))}finally{setSaving(null)}}const pct=Number(workspace?.checkpoint?.completion_percent||0);return <div className="mx-auto max-w-[1400px] p-4 sm:p-6 xl:p-8"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Habilitação</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-900">SICAF</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">Controle dos seis níveis do SICAF, com bloqueio automático enquanto o cadastro empresarial não estiver validado e rastreabilidade de cada alteração.</p></div><button onClick={()=>void loadWorkspace()} disabled={!clientId||loading} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold disabled:opacity-50">Atualizar</button></div><section className="mt-5 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><label className="text-sm font-semibold text-slate-700">Empresa<select value={clientId} onChange={e=>setClientId(e.target.value)} className="mt-2 w-full max-w-xl rounded-xl border border-slate-200 px-4 py-3 text-sm">{companies.map(c=><option key={c.client_id} value={c.client_id}>{c.display_name} — {c.company_validation_status||"sem validação"}</option>)}</select></label>{selected&&<p className="mt-2 text-xs text-slate-500">{selected.legal_name}</p>}</section>{error&&<div className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{error}</div>}{msg&&<div className="mt-4 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800">{msg}</div>}{loading?<div className="mt-5 rounded-2xl border border-slate-200 bg-white p-8 text-sm text-slate-500">Carregando habilitação SICAF...</div>:workspace&&<><section className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase text-slate-400">Cadastro empresarial</p><p className={`mt-2 text-lg font-bold ${workspace.company_validated?"text-emerald-700":"text-amber-700"}`}>{workspace.company_validated?"Validado":"Bloqueado"}</p><p className="mt-1 text-xs text-slate-500">{workspace.company_validation_status}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase text-slate-400">Progresso SICAF</p><p className="mt-2 text-2xl font-bold">{pct.toFixed(0)}%</p><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-blue-600" style={{width:`${Math.max(0,Math.min(100,pct))}%`}}/></div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase text-slate-400">Checkpoint</p><p className="mt-2 text-lg font-bold">{workspace.checkpoint?.status||"Não iniciado"}</p><p className="mt-1 text-xs text-slate-500">{workspace.checkpoint?.blocking_reason||"Sem bloqueios cadastrais."}</p></div></section>{!workspace.company_validated&&<div className="mt-5 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm leading-6 text-amber-900"><strong>SICAF bloqueado.</strong> Primeiro conclua a validação do Cartão CNPJ em Empresas. Nenhum nível pode ser aprovado antes desse gate.</div>}<section className="mt-5 space-y-3">{workspace.levels.map(level=><div key={level.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Nível {level.id}</p><h2 className="mt-1 text-lg font-bold text-slate-900">{level.name}</h2>{level.description&&<p className="mt-1 text-sm text-slate-500">{level.description}</p>}<p className="mt-2 text-xs text-slate-500">Última conferência: {level.last_checked_at?new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(level.last_checked_at)):"não realizada"}</p></div><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-bold text-slate-700">{labels[level.status]||level.status}</span></div><textarea value={notes[level.id]??""} onChange={e=>setNotes(v=>({...v,[level.id]:e.target.value}))} placeholder="Observações e evidências da conferência..." className="mt-4 min-h-20 w-full rounded-xl border border-slate-200 p-3 text-sm"/><div className="mt-3 flex flex-wrap gap-2">{["nao_iniciado","em_preparacao","pendente","atende","impeditivo"].map(s=><button key={s} disabled={!workspace.company_validated||saving===level.id} onClick={()=>void setStatus(level,s)} className={`rounded-lg border px-3 py-2 text-xs font-semibold disabled:cursor-not-allowed disabled:opacity-40 ${level.status===s?"border-blue-300 bg-blue-50 text-blue-700":"border-slate-200 bg-white text-slate-600"}`}>{labels[s]}</button>)}</div></div>)}</section></>}</div>}
