import { supabase } from "@/lib/supabase";

export type BackendDashboard={client:{id:string;legal_name:string;display_name:string|null;status:string}|null;summary:Record<string,number|string|null>;pending:Record<string,number|string|null>;opportunities:Array<Record<string,unknown>>;market:Array<Record<string,unknown>>;enrollments:Array<Record<string,unknown>>};
export async function resolveCurrentClientId():Promise<string|null>{if(!supabase)return null;const{data:auth}=await supabase.auth.getUser();if(!auth.user)return null;const selected=typeof window!=="undefined"?localStorage.getItem("uni-owner-client-id"):null;if(selected){const{data,error}=await supabase.from("clients").select("id").eq("id",selected).maybeSingle();if(!error&&data?.id)return String(data.id);localStorage.removeItem("uni-owner-client-id")}const{data:membership,error}=await supabase.from("client_members").select("client_id").eq("user_id",auth.user.id).limit(1).maybeSingle();if(error)throw error;return membership?.client_id?String(membership.client_id):null}
export async function loadCurrentClientDashboard():Promise<BackendDashboard|null>{if(!supabase)return null;const clientId=await resolveCurrentClientId();if(!clientId)return null;const{data,error}=await supabase.rpc("get_client_dashboard_backend",{p_client_id:clientId});if(error)throw error;return data as BackendDashboard}
